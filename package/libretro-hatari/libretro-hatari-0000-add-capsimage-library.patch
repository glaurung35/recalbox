--- a/Makefile.libretro	2023-08-21 22:04:29.000000000 +0200
+++ b/Makefile.libretro	2023-10-07 11:19:10.232398684 +0200
@@ -4,6 +4,13 @@
 STATIC_LINKING=0
 STATIC_LINKING_LINK=0
 
+DEBUG = 0
+IPFSUPPORT = 1
+
+ifeq ($(IPFSUPPORT), 1)
+  CAPS_LIB = -L$(STAGING_DIR)/usr/lib -lcapsimage
+endif
+
 ifeq ($(platform),)
 platform = unix
 ifeq ($(shell uname -a),)
@@ -222,6 +229,10 @@
 endif
 CFLAGS += -fsigned-char -D__LIBRETRO__ -fno-builtin
 
+ifeq ($(IPFSUPPORT), 1)
+  PLATFLAGS += -DHAVE_CAPSIMAGE=1
+endif
+
 CFLAGS   += $(fpic) $(PLATFLAGS)
 CXXFLAGS := $(CFLAGS)
 CPPFLAGS := $(CFLAGS)
@@ -241,11 +252,15 @@
 
 all: $(TARGET)
 
+ifeq ($(IPFSUPPORT), 1)
+  INCFLAGS += -I$(STAGING_DIR)/usr/include/caps
+endif
+
 $(TARGET): $(OBJECTS)
 ifeq ($(STATIC_LINKING_LINK),1)
 	$(AR) rcs $@ $(OBJECTS) 
 else
-	$(CC) $(CFLAGS) $(INCFLAGS) $(LDFLAGS) $(OBJECTS) -o $@ -lm $(ZLIB) $(SHARED)
+	$(CC) $(CFLAGS) $(INCFLAGS) $(LDFLAGS) $(OBJECTS) -o $@ -lm $(ZLIB) $(SHARED) -lpthread $(CAPS_LIB)
 endif
 
 %.o: %.c
