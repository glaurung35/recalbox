import Vue from 'vue'

export function GET_SCREENSHOTS(state, data) {
  // let screenshots = []
  //
  // Object.keys(data).map(systemKey => {
  //   return Object.keys(data[systemKey].screenshotsList).map(biosKey => {
  //     let result = data[systemKey].screenshotsList[biosKey]
  //
  //     result.system = data[systemKey].fullName
  //
  //     screenshots.push(result)
  //   })
  // })
  //
  // Object.keys(screenshots).map(key => {
  //   Vue.set(state, key, screenshots[key])
  // })
}
