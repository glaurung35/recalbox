export function _options(state) {
  return state._options
}

export function ssid (state) {
  return state.ssid
}

export function key (state) {
  return state.key
}

export function enabled (state) {
  return state.enabled
}
